name: flutter_for_dev_clean_arch
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3
  mocktail: ^0.1.4
  faker: ^2.0.0

flutter:
  uses-material-design: true

